<p id="notice"><%= notice %></p>

<h1>Judges</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @judges.each do |judge| %>
      <tr>
        <td><%= judge.name %></td>
        <td><%= link_to 'Show', admin_judge_path(judge) %></td>
        <td><%= link_to 'Edit', edit_admin_judge_path(judge) %></td>
        <td><%= link_to 'Destroy', admin_judge_path(judge), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Judge', new_admin_judge_path %>
